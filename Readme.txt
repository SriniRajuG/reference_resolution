Input: 1) SKUs (stock keeping units) of products obtained by crawling websites. 
       2) List of attributes of the data model like brand, category and unit-of-measurement

Uses the information in SKUs like product-description, to identify the attributes of data-model like brand, category etc. 

Output: ids of the attributes of the data model.


